# Claude Code instructions

@AGENTS.md

- Run everything with `uv run` from the repository root.
- Before landing: `uv run python scripts/gates.py`.
- To see the web app: `uv run chess-arena serve --home /tmp/arena-dev` and open
  http://127.0.0.1:8744.
